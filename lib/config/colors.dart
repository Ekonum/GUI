import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF007AFF);
  static const primaryLight = Color(
    0xFFE5F1FF,
  ); // Lighter shade for backgrounds/highlights
  static const secondary = Color(0xFF5AC8FA);
  static const background = Color(0xFFF8F9FA); // Light background
  static const cardBackground = Colors.white;
  static const textPrimary = Color(0xFF1D232A);
  static const textSecondary = Color(0xFF6C757D);
  static const textDisabled = Color(0xFFADB5BD);
  static const border = Color(0xFFDEE2E6);
  static const success = Color(0xFF28A745);
  static const warning = Color(0xFFFFC107);
  static const error = Color(0xFFDC3545);
  static const info = Color(0xFF17A2B8); // For info banners
  static const infoBackground = Color(
    0xFFE1F5FE,
  ); // Light background for info banners

  static const ekonumCloudBlue = Color(
    0xFF3182CE,
  ); // Placeholder for logo color
}
